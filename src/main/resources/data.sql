DROP TABLE IF EXISTS contact;

CREATE TABLE contact (contact_Id VARCHAR(250) primary key,name VARCHAR(250) NOT NULL,
                      email VARCHAR(250) NOT NULL,
                      phno  LONG NOT NULL);

